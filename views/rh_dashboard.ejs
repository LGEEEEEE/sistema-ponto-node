<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard do RH</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; margin: 2em; background-color: #f9f9f9; color: #333; }
        h1, h2 { color: #2c3e50; }
        a { color: #3498db; text-decoration: none; }
        a:hover { text-decoration: underline; }
        .header-nav { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; margin-bottom: 1em; }
        .header-nav .actions a { margin-left: 15px; }
        .btn-add { background-color: #27ae60; color: white; padding: 10px 18px; border-radius: 5px; font-size: 14px; font-weight: bold; }
        .btn-add:hover { background-color: #2ecc71; }
        .config-box { background-color: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); margin-bottom: 30px; }
        .config-form { display: flex; align-items: center; gap: 10px; }
        .config-form input { padding: 8px; border: 1px solid #ccc; border-radius: 4px; width: 80px; }
        .config-form button { padding: 8px 15px; cursor: pointer; background-color: #34495e; color: white; border: none; border-radius: 4px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); background-color: #fff; }
        th, td { border: 1px solid #ddd; padding: 12px; text-align: left; vertical-align: middle; }
        th { background-color: #f2f2f2; font-weight: bold; }
        .ferias-lista { list-style-type: none; padding: 0; margin: 10px 0 0 0; font-size: 0.9em; max-height: 100px; overflow-y: auto; }
        .ferias-lista li { background-color: #eafaf1; padding: 5px; border-radius: 3px; margin-bottom: 5px;}
        .ferias-form { display: flex; flex-direction: column; gap: 5px; align-items: flex-start; }
        .ferias-form div { display: flex; gap: 5px; align-items: center;}
        .ferias-form input { width: 125px; }
    </style>
</head>
<body>
    <div class="header-nav">
        <div>
            <h1>Dashboard do RH</h1>
            <h2>Hoje é: <%= new Date().toLocaleDateString('pt-BR', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></h2>
        </div>
        <div class="actions">
            <a href="/cadastro" class="btn-add">Adicionar Funcionário</a>
        </div>
    </div>

    <p><a href="/dashboard">← Voltar para minha dashboard</a> | <a href="/rh/relatorios">Ver Relatório de Faltas</a></p>
    
    <div class="config-box">
        <h2>Configurações Gerais</h2>
        <form action="/rh/configuracoes" method="POST" class="config-form">
            <label for="duracaoAlmocoMinutos">Duração Padrão do Almoço (em minutos):</label>
            <input type="number" name="duracaoAlmocoMinutos" id="duracaoAlmocoMinutos" value="<%= duracaoAlmocoAtual %>" min="0" required>
            <button type="submit">Salvar Configuração</button>
        </form>
    </div>

    <table>
        <thead>
            <tr>
                <th>Funcionário</th>
                <th>Expediente de Hoje</th>
                <th>Registros de Hoje</th>
                <th>Status / Horas Trabalhadas</th>
                <th>Definir Horário Padrão</th>
                <th>Férias</th>
            </tr>
        </thead>
        <tbody>
            <% if (usuarios.length > 0) { %>
                <% usuarios.forEach(usuario => { %>
                    <tr>
                        </tr>
                <% }) %>
            <% } else { %>
                <tr>
                    <td colspan="6" style="text-align: center;">Nenhum funcionário cadastrado.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>