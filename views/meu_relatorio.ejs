<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meu Relatório de Ponto</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; margin: 2em; background-color: #f9f9f9; color: #333; }
        h1, h2 { color: #2c3e50; }
        a { color: #3498db; text-decoration: none; }
        .filtro-box { background-color: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); margin-bottom: 25px; }
        .filtro-box form { display: flex; align-items: center; gap: 15px; flex-wrap: wrap; }
        .filtro-box label { font-weight: bold; }
        .filtro-box input, .filtro-box button { padding: 8px; border: 1px solid #ccc; border-radius: 4px; }
        .filtro-box button { background-color: #2980b9; color: white; cursor: pointer; }
        .semana-tabela { width: 100%; border-collapse: collapse; margin-bottom: 20px; box-shadow: 0 2px 5px rgba(0,0,0,0.05); background-color: #fff; }
        .semana-tabela th, .semana-tabela td { border: 1px solid #ddd; padding: 10px; text-align: left; vertical-align: top; }
        .semana-tabela th { background-color: #f2f2f2; font-weight: bold; }
        .semana-tabela td:first-child { font-weight: bold; width: 10%; }
        .semana-tabela ul { margin: 0; padding-left: 18px; font-size: 0.9em; }
        .observacao-falta { color: #e74c3c; font-weight: bold; }
        .observacao-férias { color: #2980b9; font-weight: bold; }
        .saldo-positivo { color: #27ae60; }
        .saldo-negativo { color: #e74c3c; }
    </style>
</head>
<body>
    <h1>Meu Relatório de Ponto</h1>
    <p><a href="/dashboard">← Voltar para a Dashboard</a></p>

    <div class="filtro-box">
        <form method="GET">
            <label for="dataInicio">De:</label>
            <input type="date" id="dataInicio" name="dataInicio" value="<%= dataInicioSelecionada %>" required>
            <label for="dataFim">Até:</label>
            <input type="date" id="dataFim" name="dataFim" value="<%= dataFimSelecionada %>" required>
            <button type="submit">Gerar Relatório</button>
        </form>
    </div>

    <% if (relatorioAgrupado) { %>
        <% if (relatorioAgrupado.semanas.length > 0) { %>
            <% relatorioAgrupado.semanas.forEach(semana => { %>
                <% const diasDaSemana = {segunda: 'Segunda-feira', terca: 'Terça-feira', quarta: 'Quarta-feira', quinta: 'Quinta-feira', sexta: 'Sexta-feira'}; %>
                <table class="semana-tabela">
                    <thead>
                        <tr>
                            <th>Dia</th>
                            <th>Data</th>
                            <th>Registros</th>
                            <th>Total</th>
                            <th>Saldo</th>
                            <th>Observação</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% Object.keys(diasDaSemana).forEach(key => { %>
                            <% const dia = semana[key]; %>
                            <tr>
                                <td><%= diasDaSemana[key] %></td>
                                <% if (dia) { %>
                                    <td><%= dia.data.toLocaleDateString('pt-BR') %></td>
                                    <td>
                                        <ul>
                                        <% dia.registros.forEach(r => { %>
                                            <li><strong><%= r.tipo %>:</strong> <%= new Date(r.timestamp).toLocaleTimeString('pt-BR') %></li>
                                        <% }) %>
                                        </ul>
                                    </td>
                                    <td><%= dia.horasTrabalhadas %></td>
                                    <td class="<%= dia.saldoHoras.startsWith('+') ? 'saldo-positivo' : 'saldo-negativo' %>"><%= dia.saldoHoras %></td>
                                    <td class="observacao-<%= dia.observacao.toLowerCase().replace(' ', '-') %>"><%= dia.observacao %></td>
                                <% } else { %>
                                    <td colspan="5" style="background-color: #f9f9f9;"></td>
                                <% } %>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% }) %>
        <% } else { %>
            <p>Nenhum registro encontrado para o período selecionado.</p>
        <% } %>
    <% } %>
</body>
</html>